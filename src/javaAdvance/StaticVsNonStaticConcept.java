package javaAdvance;

public class StaticVsNonStaticConcept {
	// Object: -> any entity that have properties(state & behavior) e.g -> car, cat, dog
	// method/function: -> behavior
	// variable: -> state
	
	// Static Vs Non Static: -> properties of a class can be static or non static
		// Static: -> with static keyword, closely linked to the class, static properties can be accessed by -
					// a. direct access
					// b. by class name
					// c. by object reference name - though non-preferred way of accessing static properites
	
		// Non-Static: -> without static keyword, closely linked to object of the class, only way to access is by 
					// a. by object reference name

	public static void main(String[] args) {
		// TODO Auto-generated method stub

	}

}
